---
all:
  children:
    gpu_instances:
      hosts:
        ec2_gpu:
          ansible_host: "{{ lookup('env', 'EC2_HOST') }}"
          ansible_user: ubuntu
          ansible_ssh_private_key_file: /root/.ssh/id_rsa
